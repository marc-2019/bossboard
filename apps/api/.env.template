# apps/api/.env — populate before running e2e demos with `npm run demo:all`
# DO NOT COMMIT THIS FILE (it's gitignored)
# Marc-task: copy `.env.template` -> `.env`, then fill in the marked sections

# === Database (matches docker-compose bossboard-postgres) ===
DATABASE_URL=postgresql://bossboard:bossboard_dev_2026@localhost:29432/bossboard
REDIS_URL=redis://localhost:29379

# === JWT (generate via `openssl rand -hex 32`) ===
JWT_SECRET=__GENERATE_RANDOM_32_CHAR__
JWT_REFRESH_SECRET=__GENERATE_RANDOM_32_CHAR__

# === API server ===
PORT=29000
NODE_ENV=development

# === Stripe (REAL test mode) — Marc to paste from stripe.com dashboard ===
STRIPE_SECRET_KEY=sk_test_PASTE_YOUR_KEY_HERE
STRIPE_WEBHOOK_SECRET=whsec_PASTE_YOUR_WEBHOOK_SECRET_HERE
STRIPE_PRICE_ID_TRADIE=price_PASTE_YOUR_PRICE_ID_HERE
STRIPE_PRICE_ID_TEAM=price_PASTE_YOUR_PRICE_ID_HERE
STRIPE_RETURN_URL=http://localhost:3000/subscription/return
MOCK_STRIPE=false

# === Anthropic Claude (real if Marc finds his key; otherwise mock) ===
# Marc: paste your key from console.anthropic.com — if you don't have it handy, set MOCK_CLAUDE=true and leave the key as the placeholder
ANTHROPIC_API_KEY=sk-ant-PASTE_YOUR_KEY_HERE
MOCK_CLAUDE=true   # set to false once your Claude key is in place

# === Resend (MOCKED — Marc has no key) ===
RESEND_API_KEY=re_mock_no_key
MOCK_RESEND=true
SMTP_FROM_NAME=BossBoard
SMTP_FROM_EMAIL=noreply@bossboard.test

# === Optional ===
BETA_MODE=true
APP_NAME=BossBoard
USE_LOCAL_LLM=false
SENTRY_DSN=
